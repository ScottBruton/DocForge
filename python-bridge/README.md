# Word COM Bridge

Python sidecar for Microsoft Word automation on Windows.

## Setup

```bash
pip install -r requirements.txt
```

## Usage

Invoked by Tauri as sidecar `word_com` with commands: `ping`, `write_document`, `read_open_document`, `export_pdf`, `update_toc`.
